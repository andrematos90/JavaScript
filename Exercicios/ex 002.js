/*
Escreva um programa em JavaScript que solicite ao usuário que insira seu nome, idade e cidade de residência. Em seguida, armazene essas informações em variáveis apropriadas e exiba-as no console.*/ 

let nome = prompt('Nome: ')
let idade = prompt('Idade: ')
let cidade = prompt('Cidade: ')

console.log(`Nome: ${nome}`)
console.log(`Idade: ${idade}`)
console.log(`Cidade: ${cidade}`)

/*
A primeira linha de código declara uma variável chamada nome e utiliza o comando prompt() para exibir uma caixa de diálogo solicitando ao usuário que insira seu nome. O valor inserido pelo usuário é armazenado na variável nome.

A segunda linha declara uma variável chamada idade e utiliza o comando prompt() para solicitar ao usuário que insira sua idade. O valor inserido pelo usuário é armazenado na variável idade.

A terceira linha declara uma variável chamada cidade e utiliza o comando prompt() para solicitar ao usuário que insira sua cidade de residência. O valor inserido pelo usuário é armazenado na variável cidade.

A quarta linha utiliza o comando console.log() para exibir uma mensagem no console, que inclui o valor armazenado na variável nome.

A quinta linha utiliza o comando console.log() para exibir uma mensagem no console, que inclui o valor armazenado na variável idade.

A sexta linha utiliza o comando console.log() para exibir uma mensagem no console, que inclui o valor armazenado na variável cidade. */