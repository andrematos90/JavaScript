As estruturas condicionais estão ligadas à tomada de decisão de um algoritmo. A
o utilizar expressões que retornam verdadeiro ou falso, o algoritmo executa o bloco de comandos relativos a este resultado.


exemplo :

var hora = 9;

if ( hora < 12 ) {

 console.log ("bom dia) 

}

else 

{ console.log ("nao é de manha") ;


--------------------------------------------------------------------------------------------------------

else if 
estrutura de decisão para ser inserida caso haja mais de duas condições.

é utlizado  para o codigo ir na ordem e executar a parte do codigo que é verdadeira ignorando e 
nao executando as que sao falsa 

var hora = 8;

if ( hora <12 ) {

 console.log ("bom dia");


} else if ( hora < 18) {

console.log("boa tarde");

} else if ( hora <= 23) {

console.log("boa noite");
}

---------------------------------------------------------------------------------------------------------------------------------------------------------------------

duas condiçoes em um if apenas e usando o operarador && ( e ) sendo assim as duas condiçoes devem ser verdadeira para o codigo ser executado


var hora = 9;

if ( hora >= 12 && hora < 18 {

console.log ("boa tarde")


----------------------------------------------------------------------------------------------------------------------------------------------------------------------

com duas condiçoes, mas apenas uma sendo verdadeira ja executa o codigo , utilizando o operardor || (ou)

var hora = 18;

if ( hora == 12 || hora == 18 ) {

console.log ("voce esta na hora do rush")

}

----------------------------------------------------------------------------------------------------------------------------------------------------------------------